# Binaries
segspec
*.exe
*.dll
*.so
*.dylib

# Test binary
*.test

# Output
*.out

# Go workspace
go.work
go.work.sum

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build
dist/
bin/

# Internal state files (Agentic PM)
IDENTITY.md
CLAUDE.md
architecture.md
features.json
progress.md
releases.json

# Internal docs
docs/plans/
docs/research/
docs/reddit-launch-posts.md
docs/SHOWOFF.md
docs/SELF-SERVE-GAPS.md

# Local-only license dev key — never commit. The Stripe webhook signer holds
# the real production private key; this file is just for local fixture-gen
# and test plumbing. If git ever starts tracking it, untrack with
# `git rm --cached internal/license/DEVELOPMENT_PRIVATE_KEY.txt` (founder
# decision — don't do it silently).
internal/license/DEVELOPMENT_PRIVATE_KEY.txt
